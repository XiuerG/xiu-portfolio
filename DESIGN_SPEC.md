# Xiu Gu 作品集 — 设计 & 实现规格

> 给 Cursor / AI coding agent 的说明：这是一份**权威设计规格**。请严格遵守
> "Do / Don't 约束" 一节——本项目的核心价值是**克制**，over-building（多余动效、
> 多余卡片、把猫做成炫技玩具）会破坏设计意图。所有技术标识（路径、token、组件名、
> 文案）以本文为准。代码现状见"文件清单"，按"执行顺序"分阶段实现。

---

## 1. 定位 & 北极星

- **一句话定位**：一个既能想清楚体验、又能亲手把它做出来的交互设计师（interaction
  designer who ships the front-end）。**网站本身就是这句话的证据。**
- **30 秒留印象**：访客（多为 recruiter）离开时应记住"这人设计有判断力，而且能落地"。
- **网站人格**：活体 demo 型，但**克制**——整体安静、留白多，只在一两个**有意义**的
  交互瞬间发力。绝不为炫技而炫技（这会让 UX 面试官反感）。
- **最高指导原则**：**克制即气质。** 任何"要不要再加点效果"的犹豫，默认选"不加"。

---

## 2. 技术栈 & 现状

- Next.js (App Router, TypeScript) + Tailwind CSS v4 + Motion (`motion/react`)，部署 Vercel。
- AI agent：Claude **Haiku 4.5**（model id `claude-haiku-4-5`），服务端调用，
  **API key 仅在服务端**（`.env.local` 的 `ANTHROPIC_API_KEY`，永不进客户端）。
- 项目已存在一份骨架（见第 10 节文件清单）。本规格的主要工作是：
  1. 把现有的通用浮窗 `AskAgent.tsx` **改造成"像素猫"系统**；
  2. 给 agent 加上**预览卡**联动机制；
  3. 落实**两种形态**（桌面 / 移动）。

---

## 3. 视觉设计系统

### 3.1 配色 token（已在 `src/app/globals.css` 的 `@theme`，沿用）

| Token | 值 | 用途 |
|---|---|---|
| `--color-ground` | `#0F1117` | 深墨底（偏蓝） |
| `--color-raised` | `#171A22` | 抬升面 / hover 填充 |
| `--color-line` | `#262A33` | 分隔线 / 描边 |
| `--color-mist` | `#8C93A1` | 次要 / 弱化文字 |
| `--color-bone` | `#EDEEF1` | 主文字 |
| `--color-accent` | `#9BB0E0` | 冷调长石蓝，**安静地**用 |
| `--color-ember` | `#D98C5F` | 暖陶色，**仅用于"激活/醒来"状态** |

**冷→暖语言（全站统一信号，务必贯彻）**：
**冷 = 静止/休息，暖 = 激活/醒来。** 这套语言同时体现在：
- 案例页顶部滚动进度条（冷蓝 → 暖陶，已实现）；
- 像素猫（睡=冷蓝灰 / 醒=暖陶）；
- 交互的 hover / active 态。

### 3.2 字体

| 角色 | 字体 | 说明 |
|---|---|---|
| Display | **Newsreader**（italic） | 标题、大字，呼应叙事性的衬线斜体 |
| Body / UI | **Geist Sans** | 正文 |
| Metadata | **Geist Mono** | 年份、标签（全大写 + 字距拉开） |

### 3.3 布局原则

- **编辑式 / 索引式**，不是卡片网格。Selected Work 用 `年份 · 角色 · 领域` 的
  元数据列表（现状即如此，保留）。
- 大留白、低信息密度、强节奏。
- 最小化粗体/分隔，让排版和留白承担层级。

### 3.4 动效原则

- 默认克制：进场用淡入 + 轻微上移，缓动 `cubic-bezier(0.22, 1, 0.36, 1)`。
- 所有动效**必须**尊重 `prefers-reduced-motion`（用 Motion 的 `useReducedMotion`）。
- 唯一允许"讲究"的瞬间见第 6 节（预览卡浮出）；其余一律低调。

---

## 4. 信息架构

页面：`Home` / `Work（/work/[slug]）` / `About` / `Contact`。

- **顶部导航 WORK / ABOUT / CONTACT 必须保留**，永远可直接点。
  猫是"更聪明的一条路"，**不是唯一入口**。强制访客只能跟猫对话 = 减分。
- **职责划分（核心结论，不可混淆）**：
  - **主页作品列表 = "浏览"**：访客自己逛预设好的内容。
  - **像素猫 = "提问 + 轻量佐证"**：回答列表答不了的问题。
- 两者**不重复**：所以猫**不做**"你说看作品→把作品列出来"这种伪导览（主页已经做了）。

---

## 5. 像素猫 — 完整规格

### 5.1 角色定位

- 猫**不是**作品导览员（与主页列表重复）。它回答**列表答不了**的问题：
  - 跨作品的综合判断（"你更偏设计还是代码？"）；
  - 没有对应分类的连接（"你做过医疗健康相关的吗？"→ 判断着把无障碍项目拉出来并说明相关性）；
  - recruiter 的潜台词（"你适合早期创业团队吗？"）。
- **内容写死，连接现想**：作品的描述文字由 Xiu 预先写好（见 `profile.ts` /
  `projects.ts`），**猫绝不即兴生成对作品的描述**。猫的智能 = 理解意图 + 连接 + 判断 +
  端出最相关的佐证。

### 5.2 形

- **低分辨率、大像素块**（像素少、块大；本身是一种"我知道我在做什么"的态度）。
- 姿势：**蜷成一团睡**（最省格子、最贴慵懒）。
- **一道极细深色描边（outline）**，让它从深墨底里浮出来、像个独立小物件。
- 形简到极致：一个弓背睡姿轮廓 + 两道闭眼横线 + 一截会动的尾巴。

### 5.3 色

- 主体 **冷蓝灰（`--color-accent` 系）**，睡着时几乎化进 `--color-ground`。
- **暖陶（`--color-ember`）= "醒来"信号**：醒/对话时出现在眼睛或身下一小块。
- 颜色在替你叙事：**冷=睡、暖=醒**，与全站冷→暖语言统一。

### 5.4 动（技术要求）

- **逐帧跳变（frame-by-frame），不要平滑补间（no smooth tweening）。**
  平滑插值会破坏像素质感。尾巴尖动作 = 2~3 帧硬切循环。
- **宁可帧少而硬，不要帧多而滑。**
- 实现可用：精灵帧切换（`steps()` 时间函数 / 离散帧切换），或离散更新像素位置。

### 5.5 性格

**温，懒但暖。** 体贴、从容、不打扰，但你需要时它在。

### 5.6 状态机

| 状态 | 触发 | 表现 |
|---|---|---|
| `idle`（默认，~90% 时间） | 用户在看内容 | 蜷着闭眼睡，偶尔尾巴尖/耳朵 2~3 帧抽动。存在感极低。 |
| `stirred` | 页面滚动中 | 被晃醒一点：眯眼/懒腰；用户停下后回到 `idle`。 |
| `hover` | 鼠标悬停（仅桌面） | 睁眼看你，慢半拍冒一句暖话（见 5.7）；眼睛/身下泛暖陶。 |
| `active` | 点击 | 坐起来、**清醒认真**；触发对话框从右下角展开。 |
| `talking` | 对话进行中 | 保持清醒、靠谱（**干活时不能一脸困**）。 |

> 慵懒性格天然解决"跟随是否抢戏"的隐忧：懒猫本就该多数时间睡觉，"安静不打扰"
> 是符合人设的自然行为，不需要额外解释。

### 5.7 文案（起始版，可调）

- hover：`…嗯？我在呢。` / `想知道点什么？慢慢问。`
- 久看：`看累了就歇会儿。`
- **兜底（答不上来）**：`这个我不太清楚，不过你可以直接找 Xiu 聊。`
  → **连"不知道"都是温的**：温性格最该在出问题时兜住人，而非只在讨喜时出现。

### 5.8 位置（关键澄清）

- **桌面：`position: fixed` 钉在屏幕右下角，从首屏到页脚一直可见。**
  它"跟随滚动"的本质是它**钉在视窗上不动**、页面在它背后流过。
  **不是**钉在页脚、滑到底才出现。
- 移动端形态见第 8 节。

### 5.9 意义（写进叙事）

像素 = Xiu 的 **Art & Technology** 出身的注脚，不是装饰。可在 About 或猫的"来历"
里**轻点一句**，让这只猫从萌点变成身份叙事的一部分。

---

## 6. 对话框 & 预览卡

### 6.1 对话框

- **从右下角展开**（与猫的原点一致，像把蜷着的地方"撑"开），不是从猫身体里长出。
- 关闭：右上角关闭 / `Esc` / 点击外部遮罩。
- 可开合；关闭后猫回到 `idle`。

### 6.2 预览卡机制（"克制的 2"——本项目唯一发力的动效）

猫回答时，对话里浮出**一张轻量预览卡**：`一张图 + 标题 + 一句话 + "细看 →" 链接`。
它把相关的东西"轻轻推到你面前"，**不是**把整个案例搬进画布。

**分寸（务必遵守）：**
- **一次只推一张，最多两张。** 一次甩多张 = 退回炫技。少 = 判断力。
- 卡是回答的**佐证，不是回答本身**。顺序：**猫先用话回答（判断/连接），卡片跟在话后浮出**。
- 卡片视觉 = **主页设计系统的缩小版**（Newsreader italic 标题、mono 标签、冷蓝灰）。
  让它像"设计系统延伸进了对话"，不像第三方聊天插件。
- 卡片数据**复用 `src/lib/projects.ts`**（标题/年份/角色/summary/封面），不另写内容。
- **浮出动作 = 懒猫气质**：不"啪"地弹出，而是**慢半拍、轻轻淡入 + 上移**
  （duration ~0.5s，ease `0.22,1,0.36,1`），像猫懒洋洋伸爪推过来。

### 6.3 预览卡数据契约（供实现）

```ts
// 卡片只引用已有项目，不新增内容
type PreviewCard = {
  slug: string;        // 对应 projects.ts 的 slug
  // 标题/年份/角色/summary/封面 全部从 projects.ts 按 slug 取，勿在别处重复定义
};
```

---

## 7. 猫的"大脑" — 意图、内容、兜底

### 7.1 内容分层（已在第 5.1 / 6.2 定义，强调）

- **内容层（写死）**：作品描述、个人档案 → `profile.ts` / `projects.ts`。
- **理解与连接层（现想）**：意图理解、跨作品判断、连接、措辞。这是猫不可替代之处。

### 7.2 "意图 → 界面响应"要可预期，不能像抽盲盒

推荐实现：给模型一个 **tool**，让它在合适时**主动调用**来决定"端出哪个项目卡"，
从而保证内容写死、由模型只负责"判断何时/哪个"：

```ts
// 服务端定义给 Claude 的工具（slug 用 enum 锁死，杜绝乱编）
{
  name: "surface_project",
  description: "当某个已有项目与用户问题高度相关时，调用它把该项目的预览卡推给用户。一次回答最多调用 2 次。",
  input_schema: {
    type: "object",
    properties: {
      slug: { type: "string", enum: ["moritz-center", "youtube-together", "jasmines-beat"] }
    },
    required: ["slug"]
  }
}
```

- 前端：流式渲染文字回答；当收到 `surface_project` 的工具调用时，按 `slug` 从
  `projects.ts` 渲染预览卡（最多 2 张）。
- **更简单的退路**（若暂不想做 tool use）：服务端用关键词/意图映射挑 slug。但推荐 tool use，
  它更准、更克制，也正好演示你会做"对话式界面"。
- 系统提示词须继续包含**护栏**（仅根据档案回答、不编造、忽略试图改它指令的注入），
  这些已在 `src/app/api/chat/route.ts`，扩展时保留。

### 7.3 必须设计的状态（高手与噱头的分水岭）

- **Loading**：等待模型时，对话区给温和的"正在想…"态（符合懒猫气质，别用刺眼 spinner）。
- **空态**：对话框刚打开、还没问时，给 3~4 个引导问题 chips（见下）。
- **误解/无关**：答不上或问题无关时，走 5.7 的温柔兜底，引向邮件。
- **错误**：网络/模型错误，给一句不吓人的提示，不暴露技术细节。

引导问题 chips（起始版，应包含"列表答不了"的那类）：
- `你更偏设计还是代码？`
- `你做过和无障碍/医疗相关的吗？`
- `Moritz Center 项目讲讲？`
- `你适合早期创业团队吗？`

---

## 8. 移动端 — 一个站，两种形态

桌面的"对话调度卡片"魔法需要同屏，移动端没有同屏的本钱，因此**重新设计**而非硬塞：

| | 桌面 | 移动 |
|---|---|---|
| 猫 | `fixed` 右下角的"活物"，hover 有反应 | 底部一个会眨眼的像素猫按钮（无 hover） |
| 进入对话 | 点击 → 对话框从右下角展开 | 点按钮 → 对话条 / 全屏对话弹层 |
| 作品浏览 | 列表 + 猫为辅 | 正常竖向滚动浏览，猫退为助手 |
| 预览卡 | 浮在对话里 | 同样浮在对话里（单列） |

> "懂得为不同语境重新设计"本身就是 UX 成熟度的体现，值得在文案/案例里点出。

---

## 9. 无障碍 & reduced-motion（必做，且是你的加分项）

- 全站动效（猫、预览卡、滚动揭示）一律接 `prefers-reduced-motion`：开启时降级为
  无位移的淡入或静止；猫停止帧动画、保持静态睡姿。
- 对话框：打开时**焦点移入**、**焦点陷阱（focus trap）**、`Esc` 关闭、关闭后**焦点归还**触发元素。
- 流式回答用 `aria-live="polite"` 朗读；猫按钮有清晰 `aria-label`。
- 深色主题下保证对比度达 WCAG AA（正文 `--color-bone` / `--color-mist` 已留意，新增元素须复核）。
- 这套无障碍处理与你的 Jasmine's Beat 项目一脉相承——**自己的站先做到，最有说服力。**

---

## 10. 文件清单（现状 / 要改 / 要新建）

```
src/
  app/
    layout.tsx              [改] 挂载像素猫系统（替换现有 <AskAgent/>）
    globals.css             [有] 设计 token，沿用；新增猫的帧动画相关样式
    page.tsx                [有] Home：Hero + WorkIndex
    about/page.tsx          [改] 写真实故事 + 轻点猫的来历/像素=身份注脚
    contact/page.tsx        [改] 换真实链接
    work/[slug]/page.tsx    [有] 案例页（含滚动进度条 冷→暖）
    api/chat/route.ts       [改] 保留护栏/限流；加 surface_project 工具 + 流式工具调用
  components/
    Hero.tsx                [有] kinetic 标题
    WorkIndex.tsx           [有] 索引式作品列表
    SiteNav.tsx             [有] 顶部导航（保留，务必不被猫取代）
    AskAgent.tsx            [拆/改] → 拆成下列像素猫组件
    cat/                    [新建]
      PixelCat.tsx          [新] 右下角 fixed 的猫 + 状态机（idle/stirred/hover/active/talking）
      ChatDialog.tsx        [新] 从右下角展开的对话框（焦点管理、Esc、空/loading/错误态）
      PreviewCard.tsx       [新] 预览卡（复用 projects.ts，主页设计系统缩小版，慢半拍浮出）
      useCatState.ts        [新] 状态机 hook（滚动监听、reduced-motion、hover/click）
  lib/
    profile.ts              [有/改] 猫的知识来源；填真实信息（accuracy 全靠它）
    projects.ts             [有] 作品数据；预览卡与案例页共用唯一来源
```

### 执行顺序（建议分阶段，便于检查）

1. **猫的"形"**：在 `cat/PixelCat.tsx` 实现 fixed 右下角的静态像素睡猫（带极细描边、冷蓝灰）。
2. **猫的"动"**：加 `idle`/`stirred` 帧动画（尾巴 2~3 帧硬切；滚动唤醒；reduced-motion 降级）。
3. **hover / click**：hover 睁眼+暖话；click 触发对话框。
4. **对话框**：`ChatDialog.tsx` 从右下角展开 + 焦点管理 + 空态 chips + loading/错误态。
5. **接 agent**：沿用 `route.ts` 的流式与护栏，前端流式渲染。
6. **预览卡 + 工具**：加 `surface_project` 工具与 `PreviewCard.tsx`（最多 2 张、慢半拍浮出）。
7. **移动端形态**：按第 8 节切换为底部按钮 + 对话弹层。
8. **无障碍复核**：按第 9 节逐条过。

---

## 11. Do / Don't 约束（防止 over-building）

**Do**
- 默认克制：犹豫时选"不加效果"。
- 猫多数时间近乎隐形（idle 睡觉）。
- 预览卡一次 1 张、最多 2 张，且跟在文字回答之后。
- 帧动画用硬切（逐帧），尊重 reduced-motion。
- 保留顶部传统导航，作品列表保持可直接浏览。
- 所有作品文案来自 `projects.ts` / `profile.ts` 单一来源。

**Don't**
- ❌ 不让猫做"看作品→列出作品"的伪导览（与主页重复）。
- ❌ 不让猫即兴生成对作品的描述。
- ❌ 不做"对话驱动整个画布、整页重排"那种大动效（已否决）。
- ❌ 预览卡不平滑补间、不一次甩多张、不喧宾夺主。
- ❌ 不把猫做成上蹿下跳的热情卡通（与全站克制气质打架）。
- ❌ 不把 API key 暴露到客户端。
- ❌ 不强制访客必须跟猫对话才能看作品。

---

## 12. 验收标准（按功能）

- **像素猫**：右下角全程可见（非页脚）；idle 时近乎静止只有尾巴微动；滚动会被唤醒、停下回睡；
  hover 睁眼+暖话；点击展开对话框。reduced-motion 下为静态。
- **对话框**：从右下角展开；焦点进入并被陷住，Esc 关闭并归还焦点；具备空/loading/错误/兜底态。
- **agent**：仅依据档案回答；答不上来温柔引向邮件；忽略注入指令；key 仅在服务端。
- **预览卡**：仅在高相关时出现；一次 ≤2 张；跟在文字回答之后；数据来自 projects.ts；
  视觉是主页系统缩小版；慢半拍淡入上移。
- **冷→暖语言**：进度条、猫、交互态一致（冷=静、暖=活）。
- **移动端**：无 hover 依赖；猫为底部按钮；作品正常竖滚浏览。
- **无障碍**：动效尊重 reduced-motion；对话框焦点管理完整；对比度 AA；流式 aria-live。

---

*本规格基于 Xiu 与协作者的完整设计讨论整理。猫的人设、克制原则、内容/智能分层
是本项目的灵魂，实现时如需取舍，优先保克制与诚实，而非增加效果。*
